<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:datagrid="clr-namespace:Syncfusion.SfDataGrid.XForms;assembly=Syncfusion.SfDataGrid.XForms"
             x:Class="TabletDemo.Views.MainPage"
             Title="{Binding Title}">

    <Grid>
        <datagrid:SfDataGrid Padding="10" AllowEditing="True" SelectionMode="Single"  NavigationMode="Cell"  AutoGenerateColumns="False" Columns="{Binding SfGridColumns}"
                                 ItemsSource="{Binding EquipoConceptos}" StackedHeaderRows="{Binding SfGridStackedHeaderRows}">
            <datagrid:SfDataGrid.Behaviors>
                <xct:EventToCommandBehavior EventName="CurrentCellEndEdit" Command="{Binding CurrentCellEndEditCommand}" />
            </datagrid:SfDataGrid.Behaviors>
        </datagrid:SfDataGrid>
    </Grid>

</ContentPage>